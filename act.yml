---

type: elasticache
action: restore
cluster: False
parameters:
  snapshot_id: "{{ snapshot-id }}"
  database_id: "{{ database-id }}"
  region: "{{ region }}"
  engine_id: "{{ engine-id }}"
  node_type: "{{ node-type }}"
